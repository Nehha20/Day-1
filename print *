#include <stdio.h>
void Accept(int iNo){
    int iCnt = 0;
    for(iCnt = 1; iCnt <= iNo; iCnt++)
    {
        printf("* ");
    }
    printf("\n");
}
int main(){
    int iVal = 0;
    printf("Enter number\n");
    scanf("%d", &iVal);
    Accept(iVal);
    return 0;
}
